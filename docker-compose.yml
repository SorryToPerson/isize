services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: isize-app
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "${APP_PORT:-3000}:3000"
    restart: unless-stopped
